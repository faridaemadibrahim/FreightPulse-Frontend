import { LaneSummary, RateTrend, RatePoint, RateLaneDetail, RateCompareData } from "@/lib/types";

export type { LaneSummary, RateTrend, RatePoint, RateLaneDetail, RateCompareData };

// Legacy Rate alias mapped to LaneSummary for backward compatibility
export type Rate = LaneSummary;
